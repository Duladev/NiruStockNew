# NiruStockNew
NiruStock

Can be need update sp procedures after run all sql scripts due to some column and data types changed as update. So should be update withing following command.
#EXEC sp_updatestats# ( SQL Server system stored procedure that updates all statistics for all user-defined and internal tables in the current database, provided at least one row has changed).
